//A program for adding two number
//here operation count = 6;
//but time complexity = O(1);
//cause if the operation count == constant then time complexity == O(1) (Order of one);

#include <stdio.h>

int main(){
    int a=10;
    int b=20;
    int sum = a + b;
    printf("%d",sum);

    return 0;
}
